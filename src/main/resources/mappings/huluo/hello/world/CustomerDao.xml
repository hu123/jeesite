<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.org.huluo.hello.dao.world.CustomerDao">
    
	<sql id="customerColumns">
		a.id AS "id",
		a.customer_name AS "customerName"
	</sql>
	
	<sql id="customerJoins">
	</sql>
    
	<select id="get" resultType="Customer">
		SELECT 
			<include refid="customerColumns"/>
		FROM customer a
		<include refid="customerJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Customer">
		SELECT 
			<include refid="customerColumns"/>
		FROM customer a
		<include refid="customerJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Customer">
		SELECT 
			<include refid="customerColumns"/>
		FROM customer a
		<include refid="customerJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO customer(
			customer_name
		) VALUES (
			#{customerName}
		)
	</insert>
	
	<update id="update">
		UPDATE customer SET 	
			customer_name = #{customerName}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM customer
		WHERE id = #{id}
	</update>
	
</mapper>