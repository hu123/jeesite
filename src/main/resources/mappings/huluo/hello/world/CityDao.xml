<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.org.huluo.hello.dao.world.CityDao">
    
	<sql id="cityColumns">
		a.id ,
		a.city_name ,
		a.country_id
	</sql>
	
	<sql id="cityJoins">
		LEFT JOIN country b ON b.id = a.country_id
	</sql>
    
	<select id="get" resultType="City">
		SELECT 
			<include refid="cityColumns"/>
		FROM city a
		<include refid="cityJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="City" parameterType="com.thinkgem.jeesite.modules.org.huluo.hello.entity.world.City">
		SELECT 
			<include refid="cityColumns"/>
		FROM city a
		<include refid="cityJoins"/>
		<where>
			
			<if test="country != null and country != ''">
				AND a.country_id = #{country.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="City">
		SELECT 
			<include refid="cityColumns"/>
		FROM city a
		<include refid="cityJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert" parameterType="com.thinkgem.jeesite.modules.org.huluo.hello.entity.world.City">
		INSERT INTO city(
			city_name,
			country_id
		) VALUES (
			#{cityName},

			 CAST(#{country.id} AS SIGNED)
		)
	</insert>
	
	<update id="update" parameterType="com.thinkgem.jeesite.modules.org.huluo.hello.entity.world.City">
		UPDATE city SET 	
			city_name = #{cityName},
			country_id = #{country.id}
		WHERE id = #{id}
	</update>
	
	<update id="delete" parameterType="com.thinkgem.jeesite.modules.org.huluo.hello.entity.world.City">
		DELETE FROM city
		<choose>
			<when test="id !=null and id != ''">
				WHERE id = #{id}
			</when>
			<otherwise>
				WHERE country_id = #{country.id}
			</otherwise>
		</choose>
	</update>
	
</mapper>